package day_01;

import java.util.Scanner;

public class Zad12 {
	
	static boolean checkPalindrom(String string) {
		String leftString = string.substring(0, string.length()/2);
		String rightString;
		
		if ((string.length() % 2) == 0) {
			for (int i = string.length()-1;i>=string.length()/2;i--)
				rightString.concat(str)
		}
		
		return false;
	}
	
	public static void main(String[] args) {
		String testo = "Kotek";
		System.out.println(testo.substring(1, 3));
		
		Scanner sc = new Scanner(System.in)
		
		System.out.println("podaj ciag znakow");
		testo = sc.nextLine();
		sc.close();
		
		
		
		
		
	}

}
